package team912.components;

class Channels {
	// channel to track number of unit blocks that have been allocated
	// should be equivalent to the starting units plus all that have been created minus 1
	final static int UnitBlockAllocationCount = 0;
	// channel to track number squad blocks that have been allocated
	final static int SquadBlockAllocationCount = 1;
}
